const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],
	darkMode : 'class',
	theme: {
		extend: {
			colors: {
				primary: '#ab51e3',
				secondary: '#f7f7f7',
			}
		}
	},

	plugins: []
};

module.exports = config;
